Put an application icon named `icon.ico` in this folder to brand the .exe and
window. It is OPTIONAL — the app and build work fine without it.

If you place ffmpeg.exe / ffprobe.exe next to the built .exe (in dist/), the
app will use them automatically for audio merge/export (see README.md).
